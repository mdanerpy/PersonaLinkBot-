PersonaLinkBot/
│
├── .gitignore              # برای نادیده گرفتن فایل‌های دیتابیس و محیط مجازی
├── README.md               # توضیح پروژه و نحوه اجرا
├── requirements.txt        # کتابخانه‌های مورد نیاز: python-telegram-bot, ...
│
├── bot.py                  # نقطه ورود اصلی برنامه (فقط ربات را اجرا می‌کند)
├── config.py               # توکن ربات و تنظیمات (از متغیرهای محیطی می‌خواند)
│
├── database/
│   ├── __init__.py
│   ├── models.py           # توابع ساخت جداول (create_tables)
│   └── db.py              # اتصال به دیتابیس و واکشی/ذخیره اطلاعات
│
├── handlers/
│   ├── __init__.py
│   ├── start.py            # مدیریت دستور /start (ثبت‌نام و خوش‌آمد)
│   ├── quiz.py             # مدیریت دستور /quiz (ارسال سوال و دریافت جواب)
│   └── profile.py          # مدیریت نمایش پروفایل
│
├── services/
│   ├── __init__.py
│   └── mbti_calculator.py  # منطق محاسبه ۴ حرف نهایی از مجموع امتیازات
│
├── data/
│   ├── questions.json      # فایل سوالات
│   └── persona_bot.db      # (ایگنور می‌شود اما اینجا ساخته می‌شود)
│
└── utils/
    ├── __init__.py
    └── helpers.py          # توابع کمکی (مثلاً ترجمه یا اعتبارسنجی)